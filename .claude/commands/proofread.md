Read the file at $ARGUMENTS.

Identify every grammar, spelling, vocabulary, or phrasing issue — the author is a non-native English speaker writing in English. For each issue explain:
- what the original text says
- what the correction is
- why it is better

Then write a corrected copy of the file to the same directory, with `_corrected` inserted before the `.md` extension (e.g. `index.md` → `index_corrected.md`). In the corrected file's front matter, set `draft = true` so Hugo will not publish it.
